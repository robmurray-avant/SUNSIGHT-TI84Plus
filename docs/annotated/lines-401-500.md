# SUNSIGHT annotated source — lines 401–500

### Line 401
```text
Goto C8
```
Jump to label C8.

### Line 402
```text
Lbl C7
```
Special case for solar right ascension when the atan denominator is zero.

### Line 403
```text
90→L₁(27)
```
Set preliminary right ascension to +90° for the zero-denominator special case.

### Line 404
```text
If L₁(25)<0
```
Test condition `L₁(25)<0`; on TI-BASIC the following single command executes only if true.

### Line 405
```text
0-90→L₁(27)
```
Set preliminary right ascension to −90° for the zero-denominator special case.

### Line 406
```text
Lbl C8
```
Continue right-ascension quadrant correction and solar declination.

### Line 407
```text
L₁(27)→L₁(28)
```
Copy preliminary right ascension for quadrant adjustment.

### Line 408
```text
If L₁(26)<0
```
Test condition `L₁(26)<0`; on TI-BASIC the following single command executes only if true.

### Line 409
```text
L₁(27)+180→L₁(28)
```
Correct right ascension quadrant when required.

### Line 410
```text
L₁(28)-360int(L₁(28)/360)→L₁(29)
```
Normalize solar right ascension to 0–360°.

### Line 411
```text
sin(L₁(24))sin(L₁(22))→L₁(69)
```
Raw inverse-sine argument for solar declination.

### Line 412
```text
L₁(69)→L₁(70)
```
Copy declination inverse-sine argument to protected working value.

### Line 413
```text
If L₁(70)>1
```
Test condition `L₁(70)>1`; on TI-BASIC the following single command executes only if true.

### Line 414
```text
1→L₁(70)
```
Clamp declination inverse-sine argument to +1 if floating-point roundoff exceeds domain.

### Line 415
```text
If L₁(70)<0-1
```
Test condition `L₁(70)<0-1`; on TI-BASIC the following single command executes only if true.

### Line 416
```text
0-1→L₁(70)
```
Clamp inverse-sine argument to −1 if floating-point roundoff exceeds domain.

### Line 417
```text
sin(L₁(70))→L₁(30)
```
Calculate solar declination.

### Line 418
```text
1.000001018(1-L₁(16)^2)/(1+L₁(16)cos(L₁(15)+L₁(17)))→L₁(31)
```
Calculate Earth–Sun distance in astronomical units for SD and parallax.

### Line 419
```text
218.3165+481267.8813L₁(10)→L₁(32)
```
Approximate lunar mean longitude used in nutation correction.

### Line 420
```text
(0-17.2sin(L₁(20))-1.32sin(2L₁(13))-.23sin(2L₁(32))+.21sin(2L₁(20)))/3600→L₁(33)
```
Approximate nutation in longitude, in degrees.

### Line 421
```text
280.46061837+360.98564736629(L₁(5)-2451545)+.000387933L₁(11)^2-L₁(11)^3/38710000→L₁(34)
```
Greenwich mean sidereal time before normalization.

### Line 422
```text
L₁(34)-360int(L₁(34)/360)→L₁(35)
```
Normalize Greenwich mean sidereal time.

### Line 423
```text
L₁(35)+L₁(33)cos(L₁(24))→L₁(36)
```
Apply equation-of-equinoxes correction to obtain apparent sidereal time.

### Line 424
```text
L₁(36)-360int(L₁(36)/360)→L₁(37)
```
Normalize Greenwich apparent sidereal time.

### Line 425
```text
L₁(37)-L₁(29)→L₁(38)
```
Compute Sun Greenwich hour angle before normalization: GAST − RA.

### Line 426
```text
L₁(38)-360int(L₁(38)/360)→L₁(39)
```
Normalize Sun GHA to 0–360°.

### Line 427
```text
L₁(39)+H→L₁(40)
```
Apply signed longitude to obtain local hour angle before normalization.

### Line 428
```text
L₁(40)-360int(L₁(40)/360)→L₁(41)
```
Normalize local hour angle to 0–360°.

### Line 429
```text
sin(G)sin(L₁(30))+cos(G)cos(L₁(30))cos(L₁(41))→L₁(42)
```
Compute sin(Hc) from latitude, declination, and LHA.

### Line 430
```text
L₁(42)→L₁(71)
```
Copy sin(Hc) to protected inverse-sine argument.

### Line 431
```text
If L₁(71)>1
```
Test condition `L₁(71)>1`; on TI-BASIC the following single command executes only if true.

### Line 432
```text
1→L₁(71)
```
Clamp Hc inverse-sine argument to +1 if needed for numerical safety.

### Line 433
```text
If L₁(71)<0-1
```
Test condition `L₁(71)<0-1`; on TI-BASIC the following single command executes only if true.

### Line 434
```text
0-1→L₁(71)
```
Clamp Hc inverse-sine argument to −1 if needed for numerical safety.

### Line 435
```text
sin(L₁(71))→L₁(43)
```
Calculate computed altitude Hc.

### Line 436
```text
sin(L₁(41))→L₁(44)
```
Azimuth numerator term.

### Line 437
```text
cos(L₁(41))sin(G)-tan(L₁(30))cos(G)→L₁(45)
```
Azimuth denominator term.

### Line 439
```text
If L₁(45)=0
```
Test condition `L₁(45)=0`; on TI-BASIC the following single command executes only if true.

### Line 440
```text
Goto C9
```
Jump to label C9.

### Line 441
```text
tan(L₁(44)/L₁(45))→L₁(46)
```
Calculate preliminary azimuth angle.

### Line 442
```text
Goto D0
```
Jump to label D0.

### Line 443
```text
Lbl C9
```
Special case for azimuth when the atan denominator is zero.

### Line 444
```text
90→L₁(46)
```
Set preliminary azimuth to +90° for the zero-denominator special case.

### Line 445
```text
If L₁(44)<0
```
Test condition `L₁(44)<0`; on TI-BASIC the following single command executes only if true.

### Line 446
```text
0-90→L₁(46)
```
Set preliminary azimuth to −90° for the zero-denominator special case.

### Line 447
```text
Lbl D0
```
Continue azimuth quadrant correction and normalize Zn.

### Line 448
```text
L₁(46)→L₁(47)
```
Copy preliminary azimuth for quadrant correction.

### Line 449
```text
If L₁(45)<0
```
Test condition `L₁(45)<0`; on TI-BASIC the following single command executes only if true.

### Line 450
```text
L₁(46)+180→L₁(47)
```
Correct azimuth quadrant when denominator is negative.

### Line 451
```text
L₁(47)+180→L₁(48)
```
Convert mathematical bearing convention to navigational Zn convention.

### Line 452
```text
L₁(48)-360int(L₁(48)/360)→L₁(49)
```
Normalize Zn to 0–360°.

### Line 454
```text
I+J/60→L₁(50)
```
Convert sextant altitude Hs from degrees/minutes to decimal degrees.

### Line 455
```text
L/60→L₁(51)
```
Convert signed index correction from minutes to degrees.

### Line 456
```text
1.76√(M)/60→L₁(52)
```
Calculate dip correction from height of eye in metres.

### Line 457
```text
L₁(50)+L₁(51)-L₁(52)→L₁(53)
```
Calculate apparent altitude Ha after index correction and dip.

### Line 459
```text
If L₁(53)>0-1
```
Test condition `L₁(53)>0-1`; on TI-BASIC the following single command executes only if true.

### Line 460
```text
Goto D1
```
Jump to label D1.

### Line 461
```text
ClrHome
```
Clear the calculator home screen before drawing the next prompt or result.

### Line 462
```text
Disp "Altitude too low"
```
Display the prompt/message “Altitude too low”.

### Line 463
```text
Disp "For refraction"
```
Display the prompt/message “For refraction”.

### Line 464
```text
Pause 
```
Pause so the user can read/review the screen; ENTER continues.

### Line 465
```text
Goto A9
```
Jump to label A9.

### Line 466
```text
Lbl D1
```
Apply refraction, semidiameter, parallax, calculate Ho, intercept, and display result.

### Line 468
```text
1/tan(L₁(53)+7.31/(L₁(53)+4.4))→L₁(54)
```
Bennett base atmospheric refraction in arcminutes from apparent altitude.

### Line 469
```text
0-.06sin(14.7L₁(54)+13)→L₁(55)
```
Bennett residual/refinement term in arcminutes.

### Line 470
```text
L₁(54)+L₁(55)→L₁(56)
```
Refined standard-atmosphere refraction in arcminutes.

### Line 471
```text
((N-80)/930)/(1+.00008(L₁(54)+39)(O-10))→L₁(57)
```
Bennett pressure/temperature correction factor.

### Line 472
```text
L₁(56)L₁(57)→L₁(58)
```
Actual refraction for entered pressure and temperature, in arcminutes.

### Line 473
```text
L₁(58)/60→L₁(59)
```
Convert refraction to degrees.

### Line 474
```text
L₁(53)-L₁(59)→L₁(60)
```
Remove atmospheric refraction from apparent altitude.

### Line 475
```text
(959.63/3600)/L₁(31)→L₁(61)
```
Calculate Sun semidiameter from Earth–Sun distance.

### Line 476
```text
If K=1
```
Test condition `K=1`; on TI-BASIC the following single command executes only if true.

### Line 477
```text
L₁(61)→L₁(62)
```
For lower-limb sight, semidiameter correction is positive.

### Line 478
```text
If K=2
```
Test condition `K=2`; on TI-BASIC the following single command executes only if true.

### Line 479
```text
0-L₁(61)→L₁(62)
```
For upper-limb sight, semidiameter correction is negative.

### Line 480
```text
L₁(60)+L₁(62)→L₁(63)
```
Convert observed limb altitude to Sun-centre altitude.

### Line 481
```text
(8.794/3600)cos(L₁(63))/L₁(31)→L₁(64)
```
Calculate solar parallax in altitude.

### Line 482
```text
L₁(63)+L₁(64)→L₁(65)
```
Apply parallax to obtain observed altitude Ho.

### Line 483
```text
60(L₁(65)-L₁(43))→L₁(66)
```
Calculate signed intercept in nautical miles/minutes: Ho − Hc.

### Line 484
```text
abs(L₁(66))→L₁(67)
```
Store intercept magnitude for display.

### Line 486
```text
ClrHome
```
Clear the calculator home screen before drawing the next prompt or result.

### Line 487
```text
Fix 1
```
Set numeric display to one decimal place for the operational result screen.

### Line 488
```text
Output(1,1,"Sun sight")
```
Write the label/message “Sun sight” at the specified screen position.

### Line 489
```text
Output(3,1,"Intercept NM")
```
Write the label/message “Intercept NM” at the specified screen position.

### Line 490
```text
Output(4,1,L₁(67))
```
Write intercept magnitude at the specified screen position.

### Line 491
```text
Output(4,10,"To")
```
Write “To” as the default direction for a non-negative intercept.

### Line 492
```text
If L₁(66)<0
```
Test whether the signed intercept is negative.

### Line 493
```text
Output(4,10,"From")
```
Replace “To” with “From” when the intercept is negative.

### Line 494
```text
Output(6,1,"Zn")
```
Write the conventional celestial-navigation azimuth label Zn.

### Line 495
```text
Output(6,6,L₁(49))
```
Write calculated Zn to one decimal place because Fix 1 is active.

### Line 496
```text
Pause 
```
Pause so the user can read the operational result; ENTER continues.

### Line 497
```text
Float
```
Restore normal floating-point display so Fix 1 cannot leak into later review screens.

### Line 498
```text
If L₁(53)<5
```
Check for an apparent altitude below 5°, where refraction becomes increasingly sensitive.

### Line 499
```text
Goto H0
```
Show the low-altitude caution when required.

### Line 500
```text
If L₁(43)>87
```
Check for Hc above 87°, where azimuth becomes unstable near the zenith.
